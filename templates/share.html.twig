{% extends 'base.html.twig' %}
{% block body %}
{#    <div class="row mt-3">#}
{#        <div class="col-8 offset-2">#}
            <div class="card bg-light">
                <div class="card-body">
                    <h1 class="text-center">Share contact</h1>

                    <table>
                        <thead>
                        <tr>
                            <th>Name</th>
                            <th>Phone</th>
                        </tr>
                        </thead>
                        <tbody>
{#                        #}
                            <tr>
                                <td>{{ contact.name }}</td>
                                <td>{{ contact.number }}</td>
                                <td>
                                    <label for="pet-select">Share with:</label>
                                    <form action="{{ path('create-share', {'id': contact.id}) }}" method="POST">
                                    <select name="owner" id="owner">
                                        <option value="">--Please choose a name--</option>
                                        {% for user in users %}
                                        <option value="{{ user.id }}">{{ user.name }}</option>
                                        {% endfor %}
                                    </select>
                                    <button type="submit" class="btn btn-info mt-4">share</button>
                                    </form>
                                </td>
                            </tr>

                        </tbody>
                    </table>

{#                    {{ form_start(form) }}#}
{#                    <div class="mt-3">#}
{#                        <label>Name</label>{{ form_widget(form.name) }}#}
{#                    </div>#}
{#                    <div class="mt-3">#}
{#                        <label>Phone number</label>{{ form_widget(form.number) }}#}
{#                    </div>#}
{#                    <button type="submit" name="contacts[save]" id="contacts_save" class="btn btn-info mt-4">SAVE</button>#}
{#                    {{ form_end(form) }}#}
                </div>
            </div>
{#        </div>#}
{#    </div>#}
{% endblock %}