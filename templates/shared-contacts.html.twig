{% extends('base.html.twig') %}
{% block body %}


<style>
    th, td {
        padding: 10px;
    }
    th {
        text-align: left;
    }
</style>
    <h1>Shared contacts</h1>
{% if contacts is not empty %}
    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Phone</th>
            <th>Sharing</th>
        </tr>
        </thead>
        <tbody>
        {% for contact in contacts %}
            <tr>
                <td>{{ contact.name }}</td>
                <td>{{ contact.number }}</td>
                <td><a href="terminate-share/{{ contact.id }}">Stop sharing</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% else %}
    <p>You do not share any contacts</p>
{% endif %}

{% endblock %}